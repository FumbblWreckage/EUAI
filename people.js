const followers = {
    window: false
}
document.getElementById("followers").addEventListener("click", () => {
    if (followers.window) {

    } else {


        followers.window = true;
    }

})